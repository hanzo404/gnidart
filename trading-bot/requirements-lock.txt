# قفل نسخه‌ها — v0.5.8 (آیتم ۷ اسپرینت).
# این فایل «دقیقاً همان چیزی» است که تست‌های سبز با آن اجرا شده‌اند
# (لینوکس/Python 3.11). نصب بازتولید‌پذیر:
#     pip install -r requirements-lock.txt
# برای ویندوز/VPS: MetaTrader5 و certifi از requirements-win.txt نصب می‌شوند؛
# نسخهٔ آن‌ها توسط نصب‌کنندهٔ ویندوز (و نه این فایل) مدیریت می‌شود.
#
# به‌روزرسانی: فقط با یک اسپرینتِ تست‌شده — هرگز وسط هفتهٔ معاملاتی.
numpy==2.4.6
pandas==3.0.5
PyYAML==6.0.3
requests==2.34.2
scikit-learn==1.9.1
scipy==1.17.1
joblib==1.6.0
threadpoolctl==3.7.0
python-dateutil==2.9.0.post0
