# STATE_AND_PATHS — منبع حقیقت پروژه (ری‌بیلد ۲۵-۰۹-۲۰۲۶)

> ⚠️ این فایل در ۲۵-۰۹ بعد از loss فضای کاری سندباکس از روی سابقهٔ سشن بازسازی شد.
> رجیستری، تصمیم‌ها و اعداد دقیق‌اند؛ اسکریپت‌های لب (us30/p3/nas100_gate) باید دوباره اضافه شوند.

> آخرین به‌روزرسانی: ۲۵ سپتامبر ۲۰۲۶ (فاز B — اعتبارسنجی دمو در حال اجرا)

## ۱) وضعیت کلان

| موضوع | وضعیت |
|---|---|
| فاز فعال | **B — go-live تک‌موتوره روی طلا (G6)** از ۰.۵٪ (کاربر ۲۴-۰۹ انتخاب کرد؛ صف شاخص‌ها متوقف) |
| تنها موتور زنده | G6 روی طلا: 3y ‏n=298/WR36٪/PF1.41/Σr+74.3R/DD−16.9R |
| سرمایهٔ شروع | ~$5,000 (بودجهٔ ۰.۵٪ = $25؛ میانهٔ ریسک $12.8 ✅) |
| حساب | فعلاً فقط دمو (MetaQuotes-Demo)؛ واقعی بعداً |
| نردبان ریسک | ۰.۵٪ ← ۰.۷۵٪ ← ۱٪؛ ارتقا فقط با پاریتی (≥۲ ماه و ~۳۰-۴۰ معامله/پله)؛ سقف ۲٪ فقط بعد از ۶ ماه پاریتی پایدار |
| سرعت واقعی | ~۸.۳ معامله/ماه ← هر پله ≈ ۴ ماه (تعداد مقید است) |
| کلید توقف v0.6 | افت ≥25R×ریسک (۱۲.۵٪ در ۰.۵٪) یا r̄₁₂ماه<0 با n≥40 ← توقف کامل؛ رفع فقط --ack-kill انسانی |
| B1-1 | ✅ پاس (۲۴-۰۹): `--dry-run --once` تمیز |
| B1-2 | 🔄 در حال اجرا: ران کاغذی پیوسته؛ اولین ورود کاغذی ۲۴-۰۹ 19:45 (خرید 0.01 @ 4289.95، SL 4269.44، TP 4341.23، RR=2.5 ✅) |

## ۲) رجیستری دروازه‌ها — چه چیزی تست شده و حکمش چیست

| استراتژی/فیلتر | حکم | دلیل |
|---|---|---|
| G6 روی طلا (تنها موتور زنده) | ✅ عبور از همهٔ دروازه‌ها | 3y: 298t / PF 1.41 / WR 36٪ / +74.3R؛ نیمه‌ها سالم |
| **G6 روی US30 (P1)** | **⛔ رد (۲۲-۰۹)** | PF 1.06؛ نیمه‌ها 1.37/0.87؛ زوال 1.80→1.26→1.15→0.69؛ بدون هزینه هم رد (1.09)؛ فید ρ0.976؛ ρ طلا +0.05 |
| **G6 روی NAS100** | **⛔ رد (۲۴-۰۹)** | n=321/PF1.08/Σr+21R؛ نیمه‌ها 1.45/0.89؛ ۲۰۲۶: PF0.77/−12.1R؛ بدون هزینه 1.10؛ فید ρ0.988؛ ρ طلا −0.15 |
| **P3-M5 پایه (طلا، دوطرفه)** | **⛔ رد (۲۴-۰۹)** | n=417/PF1.13/Σr+6.2R؛ نیمهٔ اول و ۲۰۲۶ منفی؛ فروش‌ها −3.4R |
| **P3 چلنجر الف (+فیلتر ATR)** | **⛔ رد (۲۴-۰۹)** | n=199/PF1.07/Σr+14.9R؛ بدون هزینه 1.10؛ فیلتر بهبود داد ولی نجات نداد |
| **P3H1 روی NAS100 (اکتشافی)** | **⛔ رد (۲۴-۰۹)** | n=211/PF1.07/Σr+3.4R؛ نیمهٔ دوم 0.96؛ بدون هزینه 1.21 (هزینه قاتل بود ولی حکم با هزینهٔ واقعی) |
| یافتهٔ سطح-lab | 📌 لبهٔ G6 طلاخاص است | هر ۳ تست شاخص یک امضا: نیمهٔ اول خوب/دوم خراب/۲۰۲۶ قرمز، در برابر طلای PF1.41 |
| فیلتر ساعت معاملات | ⛔ رد | روی طلا ارزشی اضافه نکرد |
| نقره (XAGUSD) | ⛔ رد (۱۸-۰۹) | نیمهٔ اول PF 0.94؛ OOS 0.73؛ بازآزموت ≥۱۲ ماه بعد |
| P2 (MR فیلترشده روی PRZ) | ⛔ رد | n=228 / PF 1.06 / Σr −34.9R؛ نیمه‌ها 0.83/1.15 ناپایدار |
| گرید / مارتینگل | ⛔ همیشه | نابودی پنهان در افت؛ نقض اصل ریسک ثابت |
| اسکالپ M1 | ⛔ | هزینه و اسپرد همهٔ لبه را می‌خورد |
| خرید EA آماده | ⛔ | MonexScalp = تجربهٔ مستقیم؛ تضاد منافع IB |
| تیون پارامتر روی پنجرهٔ اخیر | ⛔ ممنوع | آموزش دادن به نویز؛ «یک پنجره، یک شانس» |
| start.bat | ⛔ ممنوع | آستین نقرهٔ ردشده را روشن می‌کند — فقط ران تک‌نماد طلا |

## ۳) مسیرها و دیتا

- ریشهٔ ریپو (روی گیت‌هاب، شاخهٔ `arena/01a0864f-gnidart`): هر ۴ فایل `us30_m1_duka.csv.gz` (20MB)، `usatechidxusd_m1_duka.csv.gz` (22.5MB)، `ustec_m1.csv.gz`، `ustech100m_m1.csv.gz` ✅ سالم.
- `marketdata/` محلی (gitignore): کپی کاری همان ۴ فایل + دیتای طلا (`xauusd_m1_duka/mt5_3y/mt5` — نیازمند بازیابی).
- `trading-bot/`: کد بات (VPS کاربر مرجع زنده است؛ این کپی ممکن است عقب باشد).
- `.learning/` هرگز کامیت نمی‌شود.
- دانلود مطمئن از سندباکس: `gh api repos/.../contents/<path>?ref=<branch> -H "Accept: application/vnd.github.raw"`.

## ۴) ثابت‌های معماری (خلاصه)

- G6 طلا: V0 ‏(H4, rr=2.5, pad=0.50) + فقط-خرید + سشن 12-20 UTC + بلاک CHAOS + خرید فقط در TREND_UP؛ هزینه: لات 0.01، contract=100، اسلیپیج 0.05، spread_gate ‏0.40.
- G6 شاخص (US30/NAS100): همان + direction=+1 + contract=1.0 + اسلیپیج $1.0 + gate=None + pad از قاعده (نصف میانهٔ ATR14 سه‌ساله).
- رانر لایو = همان G6 (LiveRunner: سایز risk_pct از فاصلهٔ استاپ، کف 0.01 force، سقف 0.10؛ SL/TP سروری؛ state اتمیک).
- `run_live.py` فقط دمو (روی واقعی خودش متوقف می‌شود) ← پول واقعی مسیر جدا می‌خواهد (B2).
- Dukascopy: ماه صفر-مبنا؛ `USA30IDXUSD`/`USATECHIDXUSD`/`XAUUSD`؛ bi5 رکورد ۲۴بایتی، close قبل از low/high؛ scale=1000.
- بروکر GTCFX دمو: تاریخچهٔ شاخص فقط ~۳.۴ ماه (US30 از 06-10، USTEC از 06-15)؛ طلا عمیق است.

## ۵) قوانین کار با کاربر

- فقط فارسی؛ خیلی ساده و گام‌به‌گام؛ هر بار یک کار + نشانهٔ موفقیت.
- بدون VISION: خروجی متنی، نه اسکرین‌شات. دستورها را دستی تایپ کند (کپی، لینک می‌شود).
- VPS-ONLY (ویندوز، C:\trading-bot)؛ خانه = بکاپ سرد، هرگز همزمان.
- «۲۰٪/ماه» با ریاضی رد شد (×۸.۹/سال؛ نیازمند ریسک ~۱۰٪ = مرگ)؛ کاربر پذیرفت و سقف ۲٪ را انتخاب کرد.

## ۶) صف بعدی

1. B1-2: پایان ران کاغذی ← B1c: دموی واقعی تا ~۱۰-۱۵ معامله (مسیر اعتبارسنجی — دست‌نخورده ادامه دارد).
2. **تورنمنت شکار (مسیر تحقیق، تصمیم ۲۵-۰۹):** دیتای تازهٔ طلای 2020-2022 ← مسابقهٔ بستهٔ ۵ ایدهٔ قفل‌شده (بار PF≥1.3 چون ۵ شانس داریم) ← برنده فقط به پیپر فوروارد. شکار روی همان ۳ سالِ دیده‌شده ممنوع (خودفریبی چندمقایسه‌ای).
3. B2: مسیر پول واقعی (فایل جدید جدا، صفر ویرایش سورس موجود) ← شروع ۰.۵٪ (بعد از B1c).
4. بک‌لوگ: بازنویسی gate/report scripts + بکاپ اسکریپت‌های VPS-only + نقره (افق ۱۲ ماه).
